import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "digitalcollections.aucegypt.edu",        // Replace with your actual image domains
    ],
  },
};

export default nextConfig;
